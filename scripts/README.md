# scripts

Owner: **Yashwant Vadhan M**

Contains automation scripts for streaming runner execution (`run_stream.sh`), environment verification (`verify_env.py`), remote access (`setup_remote_access.sh`), mock database seeding (`seed_mock_db.py`), and one-command demo orchestration (`run_demo.sh`).

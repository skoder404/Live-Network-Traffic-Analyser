# contracts

Owner: **Yashwant Vadhan M**

Defines the single source of truth contracts across all system layers:
- `record_schema.py`: CSV traffic record specification and validator
- `serving_schema.sql`: SQLite serving store schema for all 16 analytical tables
